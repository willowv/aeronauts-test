export enum Token {
  Action = 0,
  Defense,
}

export enum Boost {
  Positive = 0,
  Negative,
}

export enum Ability {
  Perception = 0,
  Intelligence,
  Coordination,
  Agility,
  Conviction,
}

export enum Attack {
  Ranged = 0,
  Melee,
}

export enum CombatantType {
  Normal = 0,
  Dangerous,
  Tough,
  Scary,
  Player,
}
